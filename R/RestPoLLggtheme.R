
#' RestPoLLggtheme: Add RestPoLL Style and Colours to a ggplot2 Plot.
#'
#' The RestPoLLggtheme package provides three categories of important functions:
#' \code{theme}, \code{scale}, and \code{save} functions.
#'
#' @section \code{theme} function:
#'   The theme functions include a ggplot theme, \code{theme_RestPoLL}. It provides a way
#'   of setting the theme for the RestPoLL project in ggplots for use in reports,
#'   presentations, and posters. It can be set as usual after a ggplot with
#'   theme_RestPoLL(), but you can also include arguments for setting the base text size,
#'   the size of the title and size of the subtitle. See \code{\link{theme_RestPoLL}} for
#'   more info.
#'
#' @section \code{scale} functions:
#'   There are two scale functions, \code{scale_colour_RestPoLL} and \code{scale_fill_RestPoLL}. These can be used to set the colours of the points, lines and bars of the actual data in the plot to match the set project colours. See \code{\link{scale_colour_RestPoLL}} and \code{\link{scale_fill_RestPoLL}}.
#'   There are also a palette function \code{RestPoLL_pal} which shows the colours and
#'   their Hex codes of the different palettes. See \code{\link{RestPoLL_pal}}.
#'
#' @section \code{save} function: There is also a save function \code{save_restpoll_plot}
#'   which not only saves the plot as a png but further style the plot. See
#'   \code{\link{save_restpoll_plot}} for more details.
#'
#' @seealso \code{\link{ggplot2}}, \code{\link[ggplot2]{theme}},
#'   \code{\link[ggplot2]{scale_fill_manual}},
#'   \code{\link[ggplot2]{scale_colour_manual}}
#' @name RestPoLLggtheme
NULL
#> NULL
